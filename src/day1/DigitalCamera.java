package day1;

public class DigitalCamera {
    /* States
        -> Camera Type (Char)
        -> Sensor Sensitivity (String)
        -> Internal Memory (String
        -> Resolution (long)
        -> Lens (String)
        -> Storage (float)
        -> Size (char)
        -> Shutter Speed (float)
        -> Price (int)
    */
       String cameraType;
       String sensorSensitivity;
       String internalMemory;
       long resolution;
       String lens;
       float storage;
       String size;
       float shutterSpeed;
       int price;


    /* Constructors
     */

    /* Behaviours
     */
}

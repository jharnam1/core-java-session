package day1;

public class LightBulb {
    /* States
        -> Voltage (int)
        -> Color (Char)
        -> Durability (int)
        -> Size (Char)
        -> Glass Type (Char)
        -> Nominal Power (float)
        -> Socket (String)
        -> Working Temperature (float)
        -> Beam Angle (float)
    */

        int voltage;
        String color;
        int durability;
        String size;
        String glassType;
        float nominalPower;
        String socket;
        float workingTemperature;
        float beamAngle;

    /* Constructors
     */

    /* Behaviours
     */
}
